<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Chats</title>
    <link rel="stylesheet" href="style.css">    <!-- path.join provide the public css style  -->
</head>
<body>
    <h1>All Chats</h1>
    <form method="get" action="/chats/new">
        <button>Add New Chat...</button>
        <br>
        <br>
    </form>
    <% for(let chat of chats) { %>
        <div class="chat" >
            <h3>Sent By : <%= chat.from %> </h3>
            <div class="message">
                <p>Message : <i><b><%= chat.message %></b></i> </p>
                <form method="get" action="/chats/<%=chat._id%>/edit">
                    <button>Edit Message</button>
                </form>
                <br>
                <form method="post" action="/chats/<%=chat._id%>?_method=DELETE">
                    <button>Delete Chat</button>
                </form>
            </div>
            <h3>Recieved By : <i><b><%= chat.to %></b></i></h3>
            <h5>Time : <%= chat.created_at.toString().split(" ")[4] %> </h5>
            <h5>Date : <%= chat.created_at.toString().split(" ").slice(0,4).join("-") %>  </h5>
        </div>
        <hr>
    <% } %>
</body>
</html>